# TODO

## Chantier 1 — l'analyse : ce qu'il reste à écrire

L'analyse tourne sur l'appareil ; ce qu'elle fait et ce qui a été mesuré sont dans `docs/analysis.md`, la façon de le vérifier dans `docs/qualification.md`. Ce qui suit est ce qui manque.

- **Deux briques ne sont pas écrites** : l'**accent lexical** (7) et la **syllabification** (8). Sans elles, l'échelle du mot n'existe pas. La jointure (4) tourne au banc et reste à porter en Kotlin comme le reste de l'arithmétique.
- **Affiner le modèle de sons** — le remplaçant de `timit-ipa` : CTC phonémique + label priors, dense et acoustique, seul remède aux sons que la grille perd (un sur huit, prouvé irrécupérable en lecture). Plan et escalier d'oreilles dans `docs/design/dense-grid.md`, variantes dans `docs/design/finetune-brief.md`, chiffrage rendu dans `docs/design/gpu-pricing.md` et mode opératoire dans `docs/design/gpu-runbook.md`. TIMIT est acheté (LDC, complet dans `tmp/TIMIT/`, hors git) et acheminé sur Kaggle ; l'atelier tourne là-bas, une époque coûtant ~52 s sur T4 pour une tête seule.

  **La première marche de l'escalier — oreille gelée, tête seule sur `base-960h` — est condamnée, et cette fois sous la perte du papier.** La génération V1b a rejoué le balayage sur le script corrigé, contrôle sans prior compris ; le témoin décisif est ce contrôle. Sans aucun prior, le pire témoin de `faults.py` va de 0,126 à 0,221 selon l'époque et la voix, contre 0,002 à 0,004 pour le sortant `timit-ipa`, et la grille perd 37 à 48 sons contre 32. La marche n'atteint donc pas la parité avec le modèle qu'elle devait remplacer, prior ou pas : ce que la mesure condamne n'est plus le terme de régularisation mais **le régime**. Le prior reste par ailleurs monotonement destructeur (48, 53, 108 puis 238 sons perdus à l'époque 9 en montant le poids ; effondrement sur /ɪ/ à 1,00 partout à `pw1.0`), ce qui se lit sans contradiction : une tête linéaire sur des features gelées ne peut pas redistribuer la masse que l'oreille concentre sur le blank, et pénaliser le blank ne fait alors qu'aplatir. Les lignes `pw0.3` qui ne manquent aucune prise sont un artefact de survie — la grille y est si clairsemée qu'il ne reste qu'une ou deux fautes *ancrables*. Mesures brutes dans `tmp/v1b-sweep-measures.md`.

  **Tranché : on saute la marche 2 et on va à la marche 3** — affinage complet, oreille séparée, `xls-r-300m` pour dos. La marche 2 ne changeait que la taille de l'oreille sans lever le gel, or c'est le gel que le contrôle incrimine ; le partage d'oreille était une préférence et la qualité est le critère (cf. `docs/design/dense-grid.md`). Le dos est celui du sortant, ce qui fait de la comparaison l'isolat le plus propre possible : mêmes données, même dos, même alphabet, notre recette en plus. La marche 3 est aussi le seul régime où le terme de Huang et al. a jamais été montré fonctionner, donc le vrai procès du prior s'y tient. Le mode opératoire est dans `docs/design/training-handoff.md` ; ce qui qualifie reste `bench/faults.py` et `tmp/missing_mass.py`, jamais la perte.

  **Le contrôle sans prior a tourné jusqu'au bout** : `v3-pw0.0`, sur le T4 de Kaggle (notebook `speakup-training-full`). Trente époques en 4 h 18 min, les trois checkpoints écrits aux époques 009, 019 et 029, le pas d'apprentissage parcouru jusqu'à zéro sans divergence au passage du maximum. Le log porte **une** perte par époque — le dernier lot, pas une moyenne — de 3,778 à 0,129 ; elle bruite d'une époque à l'autre (0,368 puis 0,473 puis 0,387). Aucun PER n'a été lu. Rien de tout cela ne qualifie quoi que ce soit : `faults.py` et `missing_mass.py` seuls tranchent. La perte est même ici particulièrement muette sur la densité — un réseau CTC, rappelle `dense-grid.md`, « n'est payé que pour rendre la bonne séquence, jamais pour couvrir le temps » ; une perte basse atteste donc la séquence, et rien de l'étalement. Ce run porte `--prior-weight 0` : il n'a aucun terme qui pousse à la densité, et ce n'est pas ce qu'on lui demande — il teste la parité du régime.

  **L'époque 029 est qualifiée, et le contrôle passe sur la question qu'il posait.** Pire témoin de `faults.py` à **0,001**, contre **0,002** pour `timit-ipa` relu dans la même passe, et contre 0,126 à 0,221 pour la V1b. Le régime — affinage complet, oreille dégelée — atteint donc la parité sur la séparation fautes/témoins, là où les deux générations précédentes étaient condamnées. **Le balayage est autorisé : `0,1` puis `0,3`.**

  **Mais la densité recule** : 34 sons perdus contre 32 pour le sortant, et surtout la masse sous les sons manqués baisse — 18 des 34 à exactement 0,00 contre 8 des 32. Ce run porte `--prior-weight 0`, donc rien n'y pousse à la densité ; c'est la ligne de base à battre, et c'est ce que le terme de Huang et al. existe pour corriger. Le vrai procès du prior se tient au balayage.

  Deux réserves à ne pas lisser. La bande vide n'est pas uniformément meilleure — 0,058 contre 0,234 sur gb-daniel est un recul, contre 0,003 sur us-eric une amélioration ; le candidat rend en revanche la **même** bande sur les deux voix quand le sortant varie d'un facteur 78 selon l'accent. Et `09-walkin` reste manquée sur les deux voix, alors que le sortant l'attrape sur us-eric — de justesse, à 0,005 contre un témoin à 0,002. Mesures brutes dans `tmp/v3-measures.md`.

  **Le run `0,1` a tourné, et son époque 029 est lue** (notebook `speakup-training-v3-pw01`). C'est le premier run de tout le chantier où le terme de Huang et al. est réellement actif sur une oreille dégelée, donc le premier procès du prior qui ne soit pas condamné d'avance par son régime. Mesures brutes dans `tmp/v3-pw01-measures.md`.

  **Sur la séparation fautes/témoins, c'est la meilleure lecture jamais obtenue.** Sept fautes sur sept vues, sur les deux voix — `09-walkin`, que rien n'avait jamais attrapée proprement, sort à 0,126 et 0,123 contre des témoins à **0,000 exactement**, quand toute lecture précédente plafonnait à 0,001–0,002. Le sortant ne la voyait que sur us-eric, à 0,005 contre 0,002, faute et témoin se touchant. La bande de 0,126 ne se compare pas telle quelle aux 0,745 de `pw0.0-e9`, qui se calculait avec `09-walkin` manquée : à six fautes comparables elle vaut 0,712.

  **Le prior redonne de la masse, mais pas de l'étendue, et les deux mesures ne disent pas la même chose.** `missing_mass.py` revient exactement à la parité avec le sortant — 32 sons perdus contre 34 pour le contrôle, pic soi moyen 0,152 contre 0,116 — ce que le terme existe pour faire. Mais `boundaries.py` rend une durée couverte de **25,3 %**, en **recul** sur les 26,7 à 26,9 % des quatre lectures précédentes. Le prior remplit des colonnes sans les élargir. Il ne ranime pas non plus les colonnes mortes : 17 sons à exactement 0,00 contre 8 pour le sortant, presque le compte du contrôle.

  **Et sur la jointure — ce que l'app consomme — le prior est à rejeter.** `join.py` sur les quinze phrases de calibration, géométrie de jointure inchangée : **10 % de sons sans aucune lettre**, contre 7 % pour le sortant, 5 % pour le contrôle sans prior, et 6 % pour la doublure dense qui sert de cible. Six phrases sur quinze perdent un son de plus qu'avec le sortant. Là où la grille n'a pas de lettre, aucune marque ne peut se poser : c'est l'instrument terminal, et il tranche contre.

  **La cause est mesurée, et c'est l'inverse de l'effet attendu : le prior affûte la carte au lieu de l'étaler.** La mesure de **tenue** ajoutée à `boundaries.py` — l'étendue que l'alignement s'attribue lui-même, en trames, indépendante des durées de TIMIT — rend **99,4 % des sons tenus sur une seule trame de 20 ms**, contre 86,7 % pour le sortant et 88 à 90 % pour le contrôle. La durée couverte ne le montrait qu'en filigrane parce qu'elle est plafonnée par les durées vraies du corpus ; la tenue le montre en clair.

  **Les deux instruments se contredisent franchement, et ce n'est pas un paradoxe.** Une carte plus pointue sépare mieux deux répartitions au pic — d'où le 7 sur 7 de `faults.py`, le meilleur jamais obtenu — et donne moins de matière à joindre. Ce qui se déduit : **`faults.py` seul ne qualifie pas un modèle de sons**, puisqu'il récompense ce que la brique 4 paie. Toute lecture d'un candidat passe désormais par `join.py`.

  **L'époque 019 est lue, et elle va dans le même sens sans l'atteindre** : 98,7 % de sons tenus sur une trame contre 99,4 %, 8 % de sons sans lettre contre 10 %, PER de 9,5 % — le meilleur de nos checkpoints. Elle reste au-dessus du sortant sur la peakiness et en dessous sur la jointure. Son « 8 fautes sur 8 » est à lire avec soin : c'est une faute de plus **ancrable**, `08-light-right` rentrant dans la comparaison à cette époque, pas une faute de plus attrapée.

  **Les trois époques du poids `0,1` sont lues, et aucune ne passe la jointure.** 8 à 10 % de sons sans aucune lettre contre 7 % pour le sortant, et 98,7 à 99,4 % de sons tenus sur une seule trame contre 86,7 %. L'époque 9, que les deux autres points désignaient, ne renverse rien — c'est la pire des trois sur la jointure, et sa séparation fautes/témoins s'effondre par ailleurs à une bande de 0,003 avec `09-walkin` manquée. **Le poids `0,1` est donc jugé, sur ses trois points.** Le poids `0,3` n'a pas tourné, et le terme lui-même n'est pas jugé.

  **L'époque 9 rend la meilleure position jamais mesurée** — 21,0 ms d'erreur médiane de départ et 47,5 ms au 9e décile, contre 25,0 et 70,6 pour le sortant. C'est le seul instrument où le prior devance tout le reste, et il ne se déduit rien de plus : la position est ce que `boundaries.py` sait mesurer en plus de l'étendue, et elle ne dit rien de la jointure.

  **La séparation et la jointure ne classent pas les époques dans le même ordre**, ce qui est le fait de méthode de ce balayage : la 029 est la meilleure des trois sur `faults.py` et la pire sur `join.py`, la 9 l'inverse. C'est ce qui a fait entrer `join.py` dans la procédure de `qualification.md`.

  Le PER ne bouge pas — 10,2 % contre 10,1 % pour le contrôle, l'écart au sortant à 6,7 % restant celui qui n'est pas interprétable en l'état. Les insertions baissent (1,7 % contre 2,2 %), les substitutions montent (7,0 % contre 6,6 %).

  **Reste à lire pour ce run** : les époques 009 et 019, le contrôle ayant montré que l'entraînement retire après l'époque 9 ce qu'il avait ajouté. Et le balayage `0,3`, que le contrôle autorisait.

  **Les trois checkpoints sont lus, et le meilleur est le premier.** L'époque 9 rend une bande vide de **0,745 et 0,746** sur les deux voix, contre 0,234 au mieux pour `timit-ipa` et 0,058 pour l'époque 29 — sur le **même** nombre de fautes ancrables (6 sur 7), donc ce n'est pas l'artefact de grille clairsemée qui avait failli sauver les `pw0.3` de la V1b. Ses six fautes vues sont à 0,748 et au-dessus, ses témoins entre 0,000 et 0,003. Sur la densité elle bat aussi le sortant sur deux mesures (pic soi moyen 0,156 contre 0,152 ; sons à 0,20+ 9 contre 7), et lui reste inférieure sur deux autres (34 sons perdus contre 32 ; 14 à exactement 0,00 contre 8).

  **Ce que l'entraînement ajoute après l'époque 9, il le retire.** La masse se retire monotonement : pic soi moyen 0,156 → 0,147 → 0,116, sons à 0,20+ 9 → 7 → 4, sons à exactement 0,00 14 → 17 → 18. Le **nombre** de sons perdus, lui, ne suit pas (34, 33, 34) — c'est du bruit, à ne pas lire comme une tendance. Et la bande s'effondre de 0,745 à 0,058.

  **« Trente époques, c'est trop » ne tient plus : la jointure dit l'inverse des trois autres instruments.** `join.py` lu après coup sur les trois époques du contrôle rend 8, 6 puis 5 % de sons sans aucune lettre — l'entraînement améliore monotonement, quand la masse, l'étendue et la position désignaient toutes l'époque 9. **L'époque 029 du contrôle est la meilleure lecture du chantier sur la jointure, devant le sortant à 7 %.** Le nombre d'époques n'est donc pas tranché, et il ne le sera pas sur un instrument seul.

  Un fait qui limite ce qu'on peut déduire de la tenue : `v3-pw0.0-e29` est légèrement **plus** pointue que le sortant (88,3 % contre 86,7 % de sons tenus sur une trame) et lui est **supérieure** sur la jointure. La tenue n'explique donc pas à elle seule ce que la jointure trouve.

  **L'étendue des sons ne bouge pas, et c'est le résultat le plus lourd.** `bench/boundaries.py` sur 200 énoncés du split `test`, 5971 sons placés par lecture : la durée couverte est de 26,9 % pour `timit-ipa` et de 26,9 / 26,7 / 26,7 % aux époques 9, 19 et 29. Un affinage complet, à trois stades très différents, ne rend pas un millième de la durée réelle d'un son. **La peakiness est donc une propriété du régime CTC nu, pas un défaut du sortant** — la thèse de `dense-grid.md`, mesurée directement pour la première fois. Le prior reste la seule hypothèse sur la table. La position, elle, se dégrade en s'entraînant : 22,4 ms à l'époque 9, 23,5 à la 19, 25,4 à la 29 contre 25,0 pour le sortant — troisième mesure indépendante à désigner l'époque 9.

  **Le PER est mesuré, et il valide le choix de l'époque 9 sans valider l'affinage** (`bench/recognition.py`, mêmes 200 énoncés). Il ne sépare pas l'époque 9 de la 19 — 10,0 % contre 10,0 % — ce qui répond à l'inquiétude qui l'avait motivé : l'avance de l'époque 9 sur les trois autres instruments n'est pas payée en reconnaissant moins bien. En revanche nos checkpoints sont à 10,0 % quand `timit-ipa` est à 6,7 %, premier instrument où l'affinage perd franchement.

  **Cet écart de PER n'est pas interprétable en l'état, et c'est une question ouverte.** 6,7 % est très bas pour TIMIT en 39 classes (état de l'art 13 à 16 %), ce qui fait soupçonner une **fuite de split** chez `vitouphy` — invérifiable, c'est un modèle tiers. Nos runs n'ont vu que `TRAIN`, donc leur chiffre est propre et l'asymétrie joue contre nous. À trancher si l'écart devait peser sur une décision : réentraîner l'étalon nous-mêmes, ou mesurer les deux sur un corpus qu'aucun des deux n'a vu.

  **Un fait non expliqué à garder en tête** : sur TIMIT le décodage libre n'omet presque rien (0,7 % chez l'étalon, 1,3 à 1,5 % chez nous) alors que sur le jeu d'essai la grille perd un son sur huit. Parole lue et proche de l'entraînement d'un côté, synthèse ElevenLabs de l'autre — mais la différence n'est pas mesurée, et elle nuance « les poids ne voient pas ces sons » en « ils ne les voient pas *là* ».

  **`09-walkin` reste manquée aux époques 9 et 29**, sur les deux voix. Seule l'époque 19 l'attrape, sur gb-daniel, à 0,002 contre un témoin à 0,001 — faute et témoin se touchant, donc inexploitable.

  **Tous les chiffres de jointure ci-dessus sont périmés, et le choix du modèle est reporté.** Ils se calculaient au recouvrement pur, sur le compteur de trous qui ne qualifie plus. Recomptés mots-d'abord, les sons sans aucune lettre valent 8, 9 et 12 pour le sortant, le contrôle et le poids `0,1` — l'éventail allait de 12 à 23, il tient en quatre. Avec la table d'affinité : 2, 6 et 6. Sur l'étalon d'`expected.py`, seul `timit-ipa` est notable, l'annotation indexant sa grille.

  **Le critère qui déciderait n'est aucun de ces instruments : c'est leur composition.** Une marque n'arrive à l'écran que si le son existe dans la grille (`missing_mass.py`), que l'écart le sépare du témoin (`faults.py`) et qu'il porte les bonnes lettres (`join.py -s`). Les trois se mesurent sur du matériel qui partage les mêmes phrases — les cas de `faults.py` nomment le son exact et son occurrence — donc la composition est calculable, et elle ne l'a jamais été. **Fait, ni monté ni chiffré.**

  **En attendant, on garde le sortant, et c'est un report, pas un verdict.** Trois raisons : la composition n'est pas mesurée ; l'avantage de `v3-pw0.1-e29` repose sur **une** faute, `09-walkin`, la seule de la zone grise, ce qui est un `n = 1` ; et le sortant est déjà celui qui tourne, donc le garder ne coûte rien quand en changer coûte une requalification. À rouvrir quand le corpus L2 existe, pas avant et pas sur autre chose.

  **Et la tenue sort du procès de la brique 4.** Elle avait été ajoutée pour expliquer la jointure ; elle s'inversait déjà sur un cas (`v3-pw0.0-e29` est plus pointu que le sortant et lui est supérieur sur la jointure). La géométrie actuelle achève de l'en détacher : l'élargissement ne consomme que la **position** des pics, jamais l'étendue vraie. Elle garde un consommateur aux briques 7 et 8, qui lisent des durées ; elle n'en a plus à la brique 4.

  **Le point de rigueur sur l'étalon est levé** : `timit-ipa` a été relu du cache local dans la même passe que la marche 3, et rend un pire témoin de 0,002 sur les deux voix — dans la fourchette 0,002–0,004 qui n'était jusque-là que citée.
- **La localisation des sons est mesurée pour la première fois, contre les bornes de TIMIT** (`bench/boundaries.py`). L'exigence 1 de l'analyse — position et durée — n'avait aucun chiffre : `anchor.py` ne la mesure pas, il lit le modèle de **lettres**, que le choix de modèle acoustique ne touche pas. La vérité terrain était achetée et jamais lue — TIMIT annote chaque phone au niveau de l'échantillon, et on ne s'était servi que des suites de sons. Deux mesures en sortent : l'erreur de départ, bornée par la trame de 20 ms, et la **durée couverte**, qui est celle que la peakiness détruit et que le prior est censé restaurer. C'est un procès du prior plus direct que `missing_mass.py` : celui-ci dit si la masse existe, celui-là si l'**étendue** est rendue — et c'est l'étendue que l'app consomme.

- **La brique 4 tourne, et elle ne consulte aucune horloge.** La jointure se calcule de la grille et du texte seuls. La suite de sons est coupée en autant de groupes contigus que le texte a de mots — la coupe retenue étant celle qui épelle le mieux la phrase entière, un mot pouvant recevoir zéro son —, puis les lettres de chaque mot s'apparient dans son groupe sans jamais reculer. Une **table d'affinité** dit à quels sons une lettre participe, et un **son laissé sans aucune lettre coûte** : sans ce prix l'appariement ne paie que les lettres, donc rien n'empêche une voyelle de happer celles de sa voisine et de la laisser vide.

  **Mesuré** (`bench/join.py -s`, contre l'annotation d'`expected.py`, quinze phrases, 236 sons, voix `eleven-us-eric`) : **221 sons portent les bonnes lettres, soit 94 %**, et dix phrases sur quinze sont exactes. Ce qui ne s'en déduit pas : la table et le prix du son vide sont réglés sur ces quinze phrases mêmes, et rien n'a tourné ailleurs. Le prix tient sur un plateau large, de 0,7 à 3,0.

  **La table n'est pas la table graphème-phonème que l'analyse refuse.** Le refus protège le **jugement** — ne pas noter un apprenant contre une norme extérieure. La brique 4 ne juge rien, elle décide où peindre. La table ne dit jamais comme un mot se prononce, ne porte ni liste de mots ni dialecte, et répond seulement « `s` participe-t-il à /ʃ/ ». Elle a été générée **en aveugle**, par une session qui n'avait pas vu où la jointure échouait, pour qu'elle ne puisse pas y être ajustée ; le prompt est dans `tmp/affinity-prompt.md`.

  **Les quinze erreurs qui restent se concentrent sur six phrases** — `comfortable` (4), `doesnt-know` (3), `pear-tree`, `school`, `important` et `interesting` (2 chacune) ; les neuf autres n'en portent aucune. Une part n'est pas rattrapable par la jointure : `picked` perd `cked→d` parce que la grille ne décode aucun /k/, donc aucun son n'existe où poser ces lettres, et `comfortable` porte une grille dégénérée où le réseau décode /f t ɝ/ dans le désordre. L'apostrophe de `doesn't`, elle, n'a jamais été traitée.

  **La jointure est lue hors du jeu qui l'a réglée : 311 sons sur 326, soit 95 %, contre 231 sur 236 en calibration** (`bench/join.py -j heldout -s`, vingt phrases, voix `eleven-us-eric`, modèle `timit-ipa`). `ten-minutes` est déclinée avant d'être notée : son chiffre est un caractère que la table ne sait pas écrire, ce que la phrase était là pour montrer. Mesures brutes dans `tmp/heldout-measures.md`.

  **Deux règles ont été posées en cours de lecture, et elles ne valent qu'ensemble.** Une lettre que la table ne paie pas sur le son où elle atterrit n'y tient rien — sa place n'était décidée que par l'ordre du parcours. Et la table par groupe est ce qui rattrape les muettes qui appartiennent : `kn` sur /n/, `mb` sur /m/, `wr` sur /ɹ/, `dge` sur /ʤ/. Séparées, chacune est fausse ; c'est pourquoi la table par groupe a longtemps paru ne rien acheter, mesurée dans un régime où elle ne pouvait que déplacer une muette d'un voisin à l'autre. Le critère qui les départage n'est pas le nombre de sons vides mais **quelles lettres s'allument**, et là le groupe gagne neuf sons contre zéro.

  **L'annotation a été réécrite sous cette convention** : un son tient le groupe entier qui l'écrit, une lettre dont le son n'a pas été lu ne tient rien. Le critère est la **participation, jamais la position** — le `e` de `come` part, celui de `are` et de `here` reste, parce que là il écrit vraiment la voyelle.

  **Ce qui reste à faire sur la jointure, dans l'ordre :**

  - **Une lettre pour plusieurs sons — six des neuf désaccords restants, et les seuls échecs francs.** `x` de `six` et de `boxes`, `u` de `use` et de `music`. Chaque lettre choisit **un** son. Le point précédent traite ce que ça coûte à l'écran ; il ne répare pas l'appariement, qui demande un changement de modèle.
  - **Trois arbitrages de frontière, isolés.** Le /k/ unique entre `drink` et `coffee`, qui appartient au mot d'avant ou d'après sans que rien ne tranche ; le /ɝ/ de `chair` qui part sur le `i` de `is` ; le `s` de `question` donné au /ʃ/ alors qu'il écrit un /s/ que la grille n'a pas lu.
  - **`walk` reste faux, et volontairement.** Le groupe `al` sur /ɑ/ répare `walk` et casse `called`, où le `l` sonne : la table est sans contexte et ne peut pas voir que c'est la lettre suivante qui décide. Refusée par le jeu tenu à l'écart — un son gagné sur le jeu qui l'avait diagnostiquée, zéro net sur l'autre.
  - **Régler le prix du son vide sur autre chose que son propre étalon.** L'un des deux jeux règle, l'autre juge, jamais les deux.
  - **Lire une seconde voix.** `eleven-gb-daniel` change l'accent donc la grille. Coûte une seconde annotation à la main, indexée sur ses propres sons.

**Le bout en bout tourne, et il a été regardé.** `bench/turn.py` rend un tour analysé au format que l'écran consomme, `TurnFile.kt` le lit du dossier de l'app, et une prise poussée par `adb` s'affiche marquée sur le téléphone. L'arithmétique reste en Python et le dessin en Kotlin, ce qui ne change rien de ce qui se voit. Deux canaux sortent vides jusqu'aux briques 7 et 8 : l'accent et la mélodie passent par `syllables`, que rien ne calcule.

**Et ce que le tour entier montre, aucune qualification ne le montrait.** `faults.py` ne lit qu'un son par prise, celui autour duquel le jeu a été bâti ; l'écran les peint tous. Une prise dite témoin en porte cinq, une prise fautive jusqu'à dix sur quatorze. **Jugées à l'écoute par leur auteur (`bench/review.py`, verdicts dans `bench/reviews/`), quatre des cinq sont cohérentes et une incertaine — aucune incohérente.** Ce ne sont donc pas des fausses alertes : ce sont des fautes que le jeu d'essai n'avait jamais notées, faute de regarder ailleurs que sur son son.

Quatre des cinq sont de la réduction que le modèle fait et que la prise ne fait pas — `have to` dévoisé, `to` et `at` non réduits, le `t` battu de `right` articulé. **Rien ne hiérarchise ça sous une faute segmentale** : le but est de dire pareil, et un anglais qui ne réduit jamais sonne étranger d'un bout à l'autre là où un `th` dit `z` se comprend aussitôt. Toutes les fautes se marquent, aucune ne s'élit.

- **Pour la brique 7, lire le rythme de l'alignement existant** : la déformation locale de la correspondance temporelle modèle ↔ apprenant (l'un traîne sur une syllabe, avale la suivante), une fois la pente d'ensemble — le débit — retirée, est un signal de durée pour l'accent lexical. Les positions des sons des deux côtés sortent déjà de la matrice : c'est une lecture de plus, pas un calcul de plus.
- **Le seuil de marquage tient mieux qu'annoncé, et c'est le tour entier qui l'a montré.** Sur les prises étiquetées lues son par son, les sons calmes valent 0,01 à 2,2 points et la plus faible marque en vaut 25 : la bande de bruit à 5 tombe au milieu d'un vide franc. Ce qui reste ouvert n'est plus le seuil mais **la zone grise** — `09-walkin` (/ŋ/ à mi-chemin) est manqué par toutes les lectures, et cette zone bouge de 0,1 entre poste et appareil, donc elle se calibre sur la lecture de l'appareil et pas seulement sur les poids.
- **Épaissir la matière du seuil avec un corpus L2 annoté** (L2-ARCTIC, SpeechOcean762) : des milliers de fautes de vrais apprenants étiquetées au phonème, y compris la zone grise que le jeu maison n'a pas. Le montage est celui de l'app — synthétiser le modèle pour les prompts du corpus, dérouler le pipeline, regarder si l'écart sépare les phones annotés fautifs des corrects. Ça calibre le seuil à grande échelle sans enregistrer une prise, et ça dit si `09-walkin` est un cas isolé ou une famille.
- **Aucune voix modèle n'est qualifiée.** Le principe tient — un modèle n'est un étalon que s'il s'étalonne, d'autant qu'il est cru aveuglément — mais le test qui existait jugeait une voix aux notes d'un service. Il est à redéfinir contre la matrice : grille nette, pas de son écrasé, pas de zone où la répartition s'effondre. Un quatrième critère est apparu, et c'est le seul extérieur à la voix : **de combien elle diverge des autres**. Deux voix de synthèse sur les mêmes seize phrases s'écartent de 11,5 % des sons pour `eleven-us-sarah`, 13 % pour `azure-us-jenny` et 18 % pour `eleven-gb-daniel` (`tmp/voices.py`). Si la voix modèle est la source de vérité, son idiosyncrasie devient la norme qu'on fait imiter — donc un étalon devrait être une voix ordinaire.
- **Le seuil de la brique 11**, à exprimer relativement à la prise plutôt qu'en constante.
- **La stabilité de la grille** : deux rendus du même texte par la même voix doivent donner la même suite de sons, sinon la mesure n'est pas reproductible. Le cache de synthèse neutralise en partie la question, jamais entièrement.
- **Le taux de fausse alerte de l'accent sur un tour spontané.** Réglé pour le régime d'imitation (bloc F : 2 fautes sur 2, aucune fausse alerte), il reste inconnu là où l'apprenant n'a pas entendu le modèle — et il ne se mesure pas, étiqueter une prise spontanée exigeant ce modèle. Deux garde-fous à tenir en tête au moment de coder : l'emphase de sens divergera toujours d'un modèle neutre, et la parenthèse doit rendre une fausse alerte peu coûteuse.
- **L'arithmétique au-dessus de la matrice n'est pas écrite en Kotlin**, donc pas mesurée sur l'appareil ; au poste elle est négligeable devant la passe du réseau.
- **Finir de reloger les sondes.** Restent dans `tmp/bench/`, non relogés : le contrôle de f0 par autocorrélation (`audio_probe.py`, que le protocole nomme), la latence (`latency.py`) et la reconstruction (`reconstruct.py`, qui sert au chantier 2).

**Le coût du tour long est mesuré, et c'est la mémoire qui monte : 4316 Mo sur une minute d'audio.** Le temps croît comme la longueur puissance 1,3 (×0,32 sur 3 s, ×0,86 sur 60 s), la mémoire comme son carré — c'est l'attention. Un tour d'une minute plante un appareil à 4 Go. Trois issues, non départagées par la mesure et à trancher :

- **Fenêtrer la passe** — une dizaine de secondes avec recouvrement rend le coût linéaire et borne l'empreinte à celle d'une fenêtre. Ce que la couture déplace se mesure au banc gratuitement, en comparant les mêmes prises entières et fenêtrées : l'attention étant globale, une trame près d'un bord ne voit pas le même contexte.
- **Borner franchement la durée analysable** et le dire, un tour trop long portant sa raison comme n'importe quel tour non analysé.
- **Tenir que le tour d'une minute n'est pas un cas à servir.**

Indépendamment de l'issue, **les plages vides se retirent de l'audio** — de bord comme intérieures — ce qui abrège la passe plus que proportionnellement. La coupe est sûre par son **seuil de durée** : une occlusive est du silence, mais elle dure 50 à 120 ms, donc ne retirer que les plages de l'ordre de la demi-seconde place la coupe hors du domaine des phonèmes. Le seuil de **niveau**, lui, reste à poser, et il demande des tours spontanés hésitants que le banc n'a pas.

**Le runtime natif chez F-Droid est le seul point non instruit.** `onnxruntime-android` est distribué en AAR pré-compilé, ce que F-Droid n'accepte pas : leur exigence est de bâtir depuis les sources. Rien n'est fermé — ONNX Runtime est en MIT et son build Android est documenté (`./build.sh --android --build_java` produit l'AAR) — mais aucun précédent d'application F-Droid qui le compile dans sa recette n'a été trouvé, donc la voie n'est pas balisée. Trois issues :

- **Compiler ORT dans la recette F-Droid.** Propre, mais gros build, et la reproductibilité impose d'épingler la version exacte du NDK — le point délicat avec du natif.
- **Le faire entrer comme bibliothèque partagée**, compilée une fois chez eux : le mécanisme existe, il se négocie.
- **Un build minimal d'ORT**, réduit aux seuls opérateurs de notre modèle. C'est l'issue la plus prometteuse pour notre cas précis, puisqu'on ne fait tourner qu'un seul modèle : la taille et le temps de compilation tombent tous les deux, et le fichier `.onnx` exporté par `bench/export.py` est ce dont ORT tire la liste des opérateurs à conserver. Ordre de grandeur à réduire : `libonnxruntime.so` pèse 17,5 Mo pour arm64 seul.

En attendant, la sonde est tenue au build `debug` et l'AAR pré-compilé n'entre dans aucune release.

**Mesurer sur un second appareil.** Tout est mesuré sur un seul téléphone, arm64 avec instructions de produit scalaire. Rien ne dit ce que fait un appareil à 4 Go, ni un jeu d'instructions plus pauvre — et le seuil de marquage dépendant de la lecture de l'appareil, c'est une question de conception autant que de compatibilité.

## Chantier 2 — choisir les fournisseurs de conversation et de synthèse

Le montage est tranché : **chaîne STT → LLM → TTS**, reconnaissance par fichier, synthèse non pipelinée, 2,6 s jusqu'au premier son (cf. `docs/conversation-chain.md`). Reste à choisir qui tient chaque maillon — Azure Speech et DeepSeek ont servi à mesurer, pas à décider.

- **La synthèse ne doit rendre qu'un audio**, l'ancrage aux lettres se calculant sur l'appareil (cf. chantier 1, brique 4). Tout moteur est donc candidat, les libres compris, et aucun maillon n'est plus contraint par un format propriétaire.
- **Aucune voix n'est aujourd'hui qualifiée comme étalon** : le test qui existait notait une voix chez un service dont l'app ne dépend plus, il est à refaire contre la matrice (cf. chantier 1).
- **La méthode de choix est un banc, pas un tableau de prix** — un banc par maillon, jamais le couple en boîte noire. Les énoncés des deux bancs sont écrits (`docs/design/grammar-test-set.md`) : banc du juge (LLM, 25 énoncés étiquetés, fausses alertes sur l'informel correct, frontière des crans, qualité d'`intended`) et banc de l'oreille (STT, fidélité verbatim sur la faute, disfluences coupées tolérées mais mots jamais réparés, ponctuation des questions — quatre prises existantes réutilisées, deux à enregistrer).
- Contrainte commune : l'audio de chaque tour est **conservé localement**, sinon l'analyse n'a rien à examiner.
- Contrainte de fidélité : le TTS sert de **modèle à imiter** et d'**étalon de mesure**, donc qualité, **stabilité** (le même mot sonne pareil d'une fois sur l'autre) et réussite à l'étalonnage.
- Le TTS doit exposer **le choix de la voix** et accepter un rendu en haute qualité — le débit d'échantillonnage étant indifférent à l'analyse, un seul rendu sert à l'écoute et à la mesure.
- La reconnaissance par fichier coûte un sixième de la durée du tour. Assumé : le tour long est l'exception. À rouvrir seulement si l'usage montre que ça gêne.

Ni le banc du juge ni celui de l'oreille n'ont commencé. Ce sont les deux tiers restants du chantier.

## Chantier 3 — incarnation

- Porter le mécanisme de persona de `parcours` : persona = paramètre, catalogue de personas nommés, séparation cadre / voix. Le principe est provider-agnostique, l'injection ne l'est pas (`--system-prompt` du CLI chez `parcours` ; champ `system` du payload ou instructions de session ici).
- Une fois implémenté ici, il y a deux implémentations d'un même principe : matière à moisson vers une facette de sagesse commune.

## Reste

- **Icône de l'app** — aucune pour l'instant, l'app porte l'icône par défaut d'Android. `fdroid` exige par ailleurs un `icon.png` et un `featureGraphic.png` dans la fiche.
